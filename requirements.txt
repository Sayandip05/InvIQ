# ==========================================================================
# InvIQ Smart Inventory Assistant — Production Dependencies
# ==========================================================================
# All packages required to run the application in any environment.
# Dev-only tools (linters, test frameworks, debuggers, docs) live in
# requirements-dev.txt — do NOT add them here.
# ==========================================================================

# -- Web Framework ----------------------------------------------------------
fastapi>=0.104.1,<1.0.0
uvicorn[standard]>=0.24.0,<1.0.0
gunicorn>=21.2.0
python-multipart>=0.0.20

# -- GraphQL ----------------------------------------------------------------
strawberry-graphql[fastapi]>=0.235.0

# -- Database ---------------------------------------------------------------
sqlalchemy>=2.0.23,<3.0.0
psycopg2-binary>=2.9.0
alembic>=1.12.0

# -- Security & Auth --------------------------------------------------------
pyjwt>=2.8.0
pwdlib[argon2]>=0.2.0
email-validator>=2.1.0

# -- Caching & Rate Limiting ------------------------------------------------
redis>=5.0.1
upstash-redis>=1.1.0
slowapi>=0.1.9

# -- AI & LLM ---------------------------------------------------------------
langchain>=1.2.15
langchain-groq>=1.1.2
langchain-community>=0.4.1
langchain-openai>=1.2.1
langchain-core>=1.3.2
langsmith>=0.7.37
langgraph>=1.1.10

# -- Vector Store & Embeddings ----------------------------------------------
qdrant-client>=1.9.0
sentence-transformers>=3.0.0

# -- Voice / Speech-to-Text -------------------------------------------------
sarvamai>=0.1.28               # Sarvam AI — saaras:v3 STT

# -- Data Validation & Settings ---------------------------------------------
pydantic>=2.7.4
pydantic-settings>=2.1.0

# -- Office & Document Processing -------------------------------------------
openpyxl>=3.1.2                # Excel manifest uploads (vendor flow)
reportlab>=4.0.7               # PDF report generation

# -- HTTP Client ------------------------------------------------------------
# Single HTTP client: httpx covers both sync and async use-cases.
# requests has been removed — migrate any remaining calls to httpx.
httpx>=0.25.2

# -- WebSocket --------------------------------------------------------------
websockets>=14,<16             # uvicorn needs >=10.4; langgraph-sdk needs >=14,<16

# -- Configuration ----------------------------------------------------------
python-dotenv>=1.0.0           # .env file loading (supplemental to pydantic-settings)